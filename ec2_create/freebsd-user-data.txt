#!/bin/sh
sed -i .dist 's/awscli/awscli\ py27-salt\ bash\ tmux\ sudo\ vim-lite\ dual-dhclient/g'  /etc/rc.conf
echo "hostname="hostname.domain.com"" >> /etc/rc.conf
echo "ifconfig_DEFAULT="SYNCDHCP accept_rtadv"" >> /etc/rc.conf
echo ipv6_activate_all_interfaces="YES""" >> /etc/rc.conf
echo "dhclient_program="/usr/local/sbin/dual-dhclient"" >> /etc/rc.conf

mkdir -p /usr/local/etc/salt/

echo "master: 10.11.12.13" >> /usr/local/etc/salt/minion
echo "hash_type: sha512" >> /usr/local/etc/salt/minion

echo "salt_minion_enable="YES"" >> /etc/rc.conf
